[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "occam-logistics-bot"
version = "0.1.0"
description = "Paper-trading GUI bot for prediction market workflows"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
